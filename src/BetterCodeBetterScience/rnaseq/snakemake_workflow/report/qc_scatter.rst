Scatter plot of QC metrics with doublet predictions.

Shows the relationship between gene counts and UMI counts per cell,
colored by predicted doublet status. Doublets typically have elevated
counts in both dimensions.
